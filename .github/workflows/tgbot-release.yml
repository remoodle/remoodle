name: "[tgbot] Release"

on:
  push:
    branches: [trunk]
    paths:
      - "apps/telegram-bot/**"
      - ".github/workflows/tgbot-release.yml"

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: wsehl/release-please-action@main
        id: release
        with:
          config-file: .release-please/config.json
          manifest-file: .release-please/manifest.json
          path: apps/telegram-bot
          token: ${{ secrets.ROBOT_PAT }}
