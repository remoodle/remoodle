name: "[tgbot] Publish Docker image"

on:
  push:
    branches: [trunk]
    paths:
      - "apps/telegram-bot/**"
      - ".github/workflows/tgbot-docker.yml"
    tags:
      - "remoodle/telegram-bot-*"
  workflow_dispatch:

env:
  IMAGE_NAME: telegram-bot
  REGISTRY: ghcr.io

permissions: write-all

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Build image
        run: |
          IMAGE_ID=${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          if [[ "${{ github.ref }}" =~ refs/tags/* ]]; then
            GITHUB_REF=$(echo "${{ github.ref }}" | sed -e 's/.*v//')
          else
            GITHUB_REF=$(echo "${{ github.ref }}" | sed -e 's/refs\/heads\///' | sed -e 's/[\/-]/_/g')
          fi

          DOCKER_TAG="${GITHUB_REF}"

          docker build -f apps/telegram-bot/Dockerfile . --tag $IMAGE_ID:$DOCKER_TAG

          docker push $IMAGE_ID:$DOCKER_TAG
